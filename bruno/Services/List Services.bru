meta {
  name: List Services
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/services
  body: none
  auth: bearer
}

params:query {
  ~name:
  ~status:
  ~offset: 0
  ~limit: 20
}

auth:bearer {
  token: {{accessToken}}
}
