meta {
  name: Delete Service
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/projects/{{projectId}}/services/{{serviceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
