meta {
  name: Get Deployment
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/projects/{{projectId}}/services/{{serviceId}}/deployments/{{deploymentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
