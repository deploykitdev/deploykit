import {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
} from "@/components/ui/card";

export default function SettingsGeneral() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>General Settings</CardTitle>
        <CardDescription>
          Instance configuration options will appear here in a future update.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
